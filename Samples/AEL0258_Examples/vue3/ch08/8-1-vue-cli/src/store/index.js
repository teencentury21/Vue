import { createStore } from 'vuex';

const storeOptions = {
  // 狀態資料
  state: {
  },
  // 自定義組合參數
  getters: {
  },
  // 更新器
  mutations: {
  },
  // 異步處理動作
  actions: {
  },
  // vuex 模組
  modules: {
  },
};

export default createStore(storeOptions);
